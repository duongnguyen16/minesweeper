#include<bits/stdc++.h>
#include<windows.h>
using namespace std;
int main(){
    system("title Minesweeper - Close");
    cout << "Minesweeper is safe to close.";
    while(1) {
        string temp;
        cin >> temp;
    }
} // namespace std
